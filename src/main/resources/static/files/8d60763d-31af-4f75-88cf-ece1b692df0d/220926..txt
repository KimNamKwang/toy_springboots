loopback == localhost 와 127.0.0.1 두 가지.
InetAddress나 192.168.20.100 은 

IP와 포트는 연결시도할때만 필요. 인풋과 아웃풋 스트림으로 보내고 받기만 하면 됌


보조스트림은 내가 쓰고 싶은 메소드를 편하게 쓰고 싶어서 쓰는 것도 있다. 그냥 기본
스트림은 기능을 많이 제공하지 않음.

tcp에서는 나의 포트번호 자동생성. 나의 포트번호 알려면 작업관리자에서 해당 프로세스의
PID(프로세스아이디)를 보고 cmd창에서 찾아야 함.

만드는 순서(서버소켓먼저 만들어지고 클라이언트 소켓생성...), 닫는 순서 모두 중요함.

스캐너 종료순서는 아무데나 가능.

서버는 <클라이언트a, 아웃풋스트림> 을 저장하고 있어야한다. Map 형태로 기억한다.

서버는 소켓 하나당 쓰레드를 하나씩 돌리고 있다. while문을 돌리면서 다른 작업도 하느라고
쓰레드로 돌리고 있음.

필드에다가 브레이크 포인트를 걸어놓으면 그 필드가 호출될때마다 브레이크가 걸린다.








